#define  DEBUG
#include <thread\app.h>
#include <debug.h>

int      Start  (Application*,char*);

A_Start (Start,1000000);

int      Start  (Application*,char*)
{
  dout<<"Теперь всё стало работать проще и надёжнее..."<<endl;        

  return 0;
}
